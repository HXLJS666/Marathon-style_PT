# 重新构建番茄钟网站计划

## 步骤1：创建ColorConfig.json文件

* 创建一个ColorConfig.json文件

* 存储两种颜色设定：

  * 底色（baseColor）：纯黑色（#000000）

  * 高亮色（highlightColor）：C2FE0C（#C2FE0C）

## 步骤2：修改HTML文件

* 清空现有UI结构

* 创建新的基本结构：

  * 顶部200px高的顶栏，使用高亮色

  * 网页主体，使用底色作为背景

  * 保留必要的功能元素（计时器、控制按钮、设置等）

## 步骤3：修改CSS文件

* 清空现有样式

* 从ColorConfig.json加载颜色设定

* 实现基本样式：

  * 顶栏样式（高度200px，使用高亮色）

  * 网页背景（使用底色）

  * 基本布局和功能元素样式

## 步骤4：保留和调整JavaScript文件

* 保留所有功能逻辑代码

* 确保DOM元素选择器与新的HTML结构匹配

* 确保颜色相关的代码使用配置文件中的颜色

## 步骤5：测试功能

* 验证所有功能是否正常工作

* 确保颜色显示正确

* 确保顶栏和背景布局正确

